// import create from 'create-esm-loader'
// import lookup from './lookup.js'

// look up config
// create loader with config
// const config = await lookup()
// export const { resolve, getFormat, getSource, transformSource, load } = await create(config)

// Standalone
// NODE_OPTIONS="--loader esm-loader-json" node index.js

// Chainable
// NODE_OPTIONS="--loader node-esm-loader" node index.js
